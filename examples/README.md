# Examples

The release verifier and integration tests generate capsules in temporary
repositories. A user-facing generated example is produced under the target
repository's `.handoff/capsules/` directory during the completion smoke test;
that local metadata directory is intentionally ignored by Git.
